import{useEffect}from'react';import{useQuery,useQueryClient}from'@tanstack/react-query';import type{Details,ReleaseInfo,Suggestion}from'../../../shared/types/release';import type{ListType}from'../../../shared/types/lists';import{queryKeys}from'../../../shared/api/queryKeys';import{storageGetJSON,storageSetJSON}from'../../../shared/utils/storage';import{useAuthStore}from'../../auth/store/authStore';import{deleteSaved,fetchSaved,patchSavedStats,saveTitle}from'../api/saved';import{useSavedStore,type SavedItem}from'../store/savedStore';
const idOf=(id:number,type:Suggestion['mediaType'])=>`${type}:${id}`;const listsOf=(x?:ListType[])=>x?.length?Array.from(new Set(x)):['follow']as ListType[];
export function useSaved(){const user=useAuthStore(s=>s.user);const authed=useAuthStore(s=>Boolean(s.user&&s.accessToken));const guest=useSavedStore();const client=useQueryClient();const refreshGuest=guest.refreshFromAuth;useEffect(()=>{if(!authed)void refreshGuest()},[authed,refreshGuest]);const query=useQuery({queryKey:queryKeys.saved,queryFn:({signal})=>fetchSaved(signal),enabled:authed,staleTime:30_000,initialData:user?storageGetJSON<SavedItem[]>('dropdate_saved_cache_'+user.id)??undefined:undefined});const saved=authed?(query.data??[]):guest.saved;useEffect(()=>{if(user&&query.data)storageSetJSON('dropdate_saved_cache_'+user.id,query.data)},[query.data,user]);const setServer=(next:SavedItem[])=>client.setQueryData(queryKeys.saved,next);
 const addRelease=async(release:ReleaseInfo,meta:{tmdbId:number;mediaType:Suggestion['mediaType'];details?:Details},types:ListType[]=['follow'])=>{if(!authed)return guest.addRelease(release,meta,types);const before=saved;const id=idOf(meta.tmdbId,meta.mediaType);const current=saved.find(x=>x.id===id);const nextTypes=Array.from(new Set([...(current?.listTypes??[]),...listsOf(types)]));const optimistic:SavedItem=current?{...current,listTypes:nextTypes}:{...release,id,tmdbId:meta.tmdbId,mediaType:meta.mediaType,savedAt:Date.now(),details:meta.details,listTypes:nextTypes};setServer(current?saved.map(x=>x.id===id?optimistic:x):[...saved,optimistic]);try{for(const type of types)await saveTitle(release,{id:meta.tmdbId,title:release.title,mediaType:meta.mediaType,posterUrl:release.posterUrl},type);await client.invalidateQueries({queryKey:queryKeys.saved});await client.invalidateQueries({queryKey:queryKeys.achievements})}catch(e){setServer(before);throw e}};
 const removeRelease=async(id:string,type?:ListType)=>{const existing=saved.find(x=>x.id===id);if(!existing)return;if(!authed)return guest.removeRelease(id,type);const before=saved;setServer(type?saved.map(x=>x.id===id?{...x,listTypes:x.listTypes.filter(t=>t!==type)}:x).filter(x=>x.listTypes.length):saved.filter(x=>x.id!==id));try{await deleteSaved(existing.tmdbId,existing.mediaType,type);await client.invalidateQueries({queryKey:queryKeys.saved})}catch(e){setServer(before);throw e}};
 const updateStats=async(item:Suggestion,type:ListType,stats:{userRating?:number;watchCount?:number;lastWatchedAt?:string})=>{if(!authed)return guest.updateStats(item,type,stats);const before=saved;setServer(saved.map(x=>x.id===idOf(item.id,item.mediaType)?{...x,...stats}:x));try{await patchSavedStats(item,type,stats);await client.invalidateQueries({queryKey:queryKeys.saved})}catch(e){setServer(before);throw e}};
 const setListTypes=async(item:Suggestion,types:ListType[],meta?:{release?:ReleaseInfo;details?:Details})=>{const existing=saved.find(x=>x.id===idOf(item.id,item.mediaType));const current=existing?.listTypes??[];const add=types.filter(x=>!current.includes(x));const remove=current.filter(x=>!types.includes(x));if(meta?.release&&add.length)await addRelease(meta.release,{tmdbId:item.id,mediaType:item.mediaType,details:meta.details},add);for(const type of remove)await removeRelease(idOf(item.id,item.mediaType),type)};
 const findByTmdbId=(id:number,type:Suggestion['mediaType'])=>saved.find(x=>x.id===idOf(id,type));const getListTypes=(item:Suggestion)=>findByTmdbId(item.id,item.mediaType)?.listTypes??[];return{saved,isLoading:authed?query.isLoading:guest.isLoading,addRelease,removeRelease,updateStats,setListTypes,findByTmdbId,getListTypes,isSuggestionSaved:(item:Suggestion)=>Boolean(findByTmdbId(item.id,item.mediaType))}}

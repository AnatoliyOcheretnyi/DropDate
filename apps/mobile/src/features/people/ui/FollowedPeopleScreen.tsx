import { Text,View } from 'react-native';import { useQuery } from '@tanstack/react-query';import { useRouter,type Href } from 'expo-router';import { FeatureScreen,featureStyles as s } from '../../../shared/ui/FeatureScreen';import { ScreenState } from '../../../shared/ui/ScreenState';import { MotionPressable } from '../../../shared/ui/MotionPressable';import { queryKeys } from '../../../shared/api/queryKeys';import { getFollows } from '../api/people';export function FollowedPeopleScreen(){const router=useRouter();const query=useQuery({queryKey:queryKeys.followedPeople,queryFn:({signal})=>getFollows(signal)});if(query.isLoading)return <ScreenState loading title="Оновлюємо людей"/>;if(query.isError)return <ScreenState title="Список недоступний" message={query.error.message} onRetry={()=>query.refetch()}/>;return <FeatureScreen title="Улюблені люди" subtitle="Актори й режисери, за якими ти стежиш.">{query.data?.length?query.data.map(item=><MotionPressable key={`${item.personId}:${item.role}`} style={s.card} onPress={()=>router.push(`/person/${item.personId}` as Href)}><Text style={s.heading}>{item.name}</Text><Text style={s.text}>{item.role==='actor'?'Актор':'Режисер'} · {item.subscribed?'Сповіщення ввімкнено':'В улюблених'}</Text></MotionPressable>):<View style={s.card}><Text style={s.heading}>Список порожній</Text><Text style={s.text}>Відкрий актора або режисера зі сторінки фільму.</Text></View>}</FeatureScreen>}
